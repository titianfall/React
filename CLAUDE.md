# CLAUDE.md

《소플의 처음 만난 리액트》로 리액트를 공부하는 개인 학습 저장소.
책의 흐름을 따라가되, 개념마다 [ko.react.dev](https://ko.react.dev) 공식 문서를 함께 읽고
최신 권장 방식으로 다시 써보는 것이 원칙이다. 상세 배경은 [README.md](README.md) 참고.

## 커밋 규칙

- **`Co-Authored-By` 트레일러를 붙이지 않는다.**
- **`🤖 Generated with ...` 같은 생성·협업 태그를 붙이지 않는다.** PR 본문도 마찬가지.
- 커밋 메시지 형식:
  ```
  ch05: props 전달 예제 실습
  ch07: useEffect 정리 노트 추가
  docs: README 진도표 갱신
  ```
- **`README.md` 갱신은 노트·코드 변경과 반드시 별도 커밋으로 분리한다.**
- 커밋·푸시는 사용자가 요청할 때만 한다.

## 저장소 구조

```
React/
├─ CLAUDE.md
├─ README.md          # 저장소 소개 · 진도표
├─ notes/
│  ├─ README.md       # 30초 요약 인덱스 (모든 장의 요약 모음)
│  └─ chXX.md         # 장별 정리 노트
└─ my-app/            # Vite + React 앱 (현재는 create vite 스캐폴딩 그대로)
```

## 노트(notes) 규칙

장별 노트 `notes/chXX.md`는 아래 순서를 지킨다.

1. `# X장 제목`
2. **`⏱ 30초 요약`** — 최상단. 5줄 이내이고, **각 줄이 본문 해당 절로 링크**되어야 한다.
3. `Preview — 이 장의 지도` (mermaid 마인드맵)
4. 본문 (책의 절 번호 그대로)
5. `이 장에서 배운 것 / 헷갈린 것`
6. `함께 읽을 공식 문서`
7. **하단 이전/다음 장 네비게이션**

지킬 것:

- **`notes/README.md`는 모든 장의 30초 요약을 모은 인덱스다. 노트를 추가·수정하면 여기도 같이 갱신한다.**
  요약이 두 곳에서 어긋나면 안 된다. `notes/README.md`의 요약은 노트 본문 절로 직접 링크한다.
- 장 사이에 개념이 이어지면 본문에서 **상대 경로 링크로 연결**한다. (예: 1장에서 SPA를 언급하면 `[0장 SPA](./ch00.md#4-spa-single-page-application)`)
- 책의 그림은 mermaid 다이어그램 · 표 · ASCII 아트로 적극적으로 재현한다.
- 사진·본문이 없어 추정으로 채운 부분은 📸 표시와 함께 명시한다. 임의로 단정하지 않는다.
- 책이 클래스 컴포넌트로 설명하는 부분은 Hook 버전도 함께 적어 비교한다.
- 책과 최신 React가 다른 점은 노트 하단 표와 `README.md`의 "책과 최신 React의 차이 메모"에 모두 남긴다.

## 코드 규칙

- **`my-app/`은 현재 `npm create vite`로 만든 기본 스캐폴딩 그대로다. 직접 작성한 실습 코드가 아니므로
  노트와 엮지 않는다.** 노트에서 스캐폴딩 파일(`App.jsx`, `main.jsx` 등)로 링크를 걸지 말 것.
- 직접 작성한 실습 코드가 생기면 그때 노트와 연결한다. 그 전까지 노트는 **문서만으로 완결**되게 쓴다.
- 실행: `cd my-app && npm run dev` (http://localhost:5173)
- 빌드 도구는 **Vite**. 책의 `create-react-app`은 개념만 다루고 실제로는 쓰지 않는다.

## 문서 작성 규칙

- 문서는 **한국어**로 쓴다. 기술 용어는 처음 등장할 때 원어를 병기한다. (예: 가상 DOM(Virtual DOM))
- 저장소 내부 파일은 **상대 경로 마크다운 링크**로 건다. 절대 경로·백틱 경로 표기는 쓰지 않는다.
- 링크를 걸기 전에 대상 파일과 앵커가 실제로 존재하는지 확인한다.
